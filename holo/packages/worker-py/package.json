{
  "name": "@holo/worker-py",
  "private": true,
  "scripts": {
    "dev": "python -m worker.worker",
    "format": "python -m black . || true",
    "lint": "python -m ruff check . || true",
    "build": "echo 'Python worker: build step is containerization'",
    "typecheck": "python -m mypy . || true"
  }
}

# Docs

## Diagrams

Mermaid sources live under `docs/diagrams/*.mmd`.

To (re)generate PNGs locally:

```bash
bash scripts/render_diagrams.sh
```

This uses `npx @mermaid-js/mermaid-cli` (so Node.js is required).
